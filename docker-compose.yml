services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "8081:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev -- --host 0.0.0.0 --port 5173

